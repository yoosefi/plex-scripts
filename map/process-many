#!/bin/bash
process="$(dirname "$(realpath "${0}")")/process"
for x in "${@}"; do
    if [ -d "${x}" ]; then
        cd "${x}" || continue
        echo "${x}"
        $process
        cd - > /dev/null
    fi
done

