#!/bin/bash

for x in "${@}"; do
    if [ -f "${x}" ]; then
        echo "${x}" >> .plexignore
    elif [ -d "${x}" ]; then
        echo "${x}/*" >> .plexignore
    fi
done

sort -u -o .plexignore .plexignore

